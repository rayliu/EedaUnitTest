package eeda.login;import com.thoughtworks.selenium.Selenium;import eeda.leads.LeadsListPage;import eeda.property.ClientListPage;public class HomePage {	private Selenium selenium;	public HomePage(Selenium selenium) {		this.selenium = selenium;		if (!selenium.getLocation().equals("http://localhost:8080/")) {			throw new IllegalStateException("This is not Home Page of logged in user, current page" + "is: " + selenium.getLocation());		}	}	public HomePage manageProfile() {		// Page encapsulation to manage profile functionality		return new HomePage(selenium);	}	public LeadsListPage openLeadsList() {		selenium.open("/list");		selenium.waitForPageToLoad("5000");		return new LeadsListPage(selenium);	}	public ClientListPage openPropertyClientList() {		selenium.open("/propertyClient");		selenium.waitForPageToLoad("5000");		return new ClientListPage(selenium);	}}